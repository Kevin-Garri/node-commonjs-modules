// names.js

// Funzione che accetta due parametri: firstName e lastName e restituisce un oggetto
const createName = (firstName, lastName) => (
  {
    firstName,
    lastName
  }
)

// Esportazione della funzione
module.exports = createName;
